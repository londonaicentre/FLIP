.. This page is a thin wrapper. The body is flip-utils/docs/overview.rst, which doubles as the
   PyPI long_description (flip-utils/pyproject.toml: readme = {file = "docs/overview.rst"}), so it
   is included rather than copied to keep a single source of truth. Keep that file free of
   Sphinx-only roles — twine renders it as plain RST.

.. include:: ../../../flip-utils/docs/overview.rst
