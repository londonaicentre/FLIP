FL_BASE_IMAGE_TAG=stag
RUNS_DIR=./runs
NUM_CLIENTS=2
# GPU device id(s) the NVFLARE simulator assigns to the client workers (comma-separated).
# Defaults to 0; set e.g. SIM_GPU=0,1 on a multi-GPU host.
# SIM_GPU=0