# Tutorial-local artifacts (regenerated or downloaded; not committed).
# Processed model weights (*.pt) are ignored globally at the repo root.
fl_job/
uv.lock
models/
# `make experiment`/`make package` result bundles (sent back for comparison, not committed)
arkplus_sim_experiment_*
