fl_job/
# The perceptual loss's backbone and the hub layout it is staged into: produced locally by
# `make weights` from the shared download, never committed (FLIP#1206).
app_files/squeezenet1_1-b8a52dc0.pth
app_files/torch_hub/
# Client-local debug images (SAVE_DEBUG_SAMPLES); patient-derived, never committed.
app_files/debug_samples/
