boto3>=1.38.31
monai>=1.4.0
nibabel>=5.3.2  # 3-D helpers only (detect_axial_anisotropy)
nvflare==2.9.0
pandas>=2.3.0
pydantic>=2.11.7
pydantic-settings>=2.10.1
gdown
einops  # 3-D helpers only (slice_volume_for_2d)
pydicom
matplotlib
# Needed by debug_samples.py (`torchvision.utils.make_grid`). Must match the base image's torch
# (>=2.11, cu128); the authoritative pin lives in flip-utils/pyproject.toml. Note this is no longer
# pulled in for a perceptual loss: that is MedicalNet now (medicalnet_perceptual.py), which loads a
# shipped checkpoint rather than a hub-backed backbone. `lpips` went with it — it was unused, and an
# unused hub-backed loss in the spec is exactly what the offline guard (FLIP#1206) exists to stop.
torchvision>=0.26.0
