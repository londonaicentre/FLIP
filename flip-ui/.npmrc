; FLIP supply-chain cooldown — never install an npm release younger than 3
; days (72h), so a poisoned package can be yanked before it is consumed.
; Requires npm >= 11.10; older npm silently ignores this key.
; See CONTRIBUTING.md ("Dependency cooldown").
min-release-age=3
